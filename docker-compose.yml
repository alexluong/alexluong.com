services:
  app:
    image: alexluong/personal:alexluong.com-5.1
    restart: unless-stopped
    ports:
      - "8090:8090"
    volumes:
      - type: bind
        source: ./data
        target: /pb_data
