services:
  app:
    image: alexluong/personal:alexluong.com-${VERSION}
    restart: unless-stopped
    environment:
      - VERSION=${VERSION}
    ports:
      - "8090:8090"
    volumes:
      - type: bind
        source: ./data
        target: /pb_data
